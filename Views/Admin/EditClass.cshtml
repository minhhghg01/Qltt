@model Qltt.ViewModels.EditClassViewModel

@{
    ViewData["Title"] = "EditClass";
}

<div class="container mt-4">
    <h2 class="text-center">Sửa thông tin lớp học</h2>
    <form asp-action="EditClass" method="post">
        <input type="hidden" name="ClassId" value="@Model.ClassId" />

        <div class="form-group">
            <label>Tên lớp</label>
            <input type="text" name="ClassName" value="@Model.ClassName" class="form-control" />
        </div>

        @* <div class="form-group">
            <label>Chọn giáo viên</label>
            <select name="TeacherId" class="form-control">
                <option value="">-- Không có --</option>
                @if (Model.Teachers != null && Model.Teachers.Count > 0)
                {
                    foreach (var teacher in Model.Teachers)
                    {
                        <option value="@teacher.TeacherId">
                            @teacher.User.FirstName @teacher.User.LastName (@teacher.User.Email)
                        </option>
                    }
                }
                else
                {
                    <option value="">Không có giáo viên nào khả dụng</option>
                }
            </select>
        </div> *@

        <div class="mt-3">
            <button type="submit" class="btn btn-primary">Lưu</button>
            <a asp-action="ManageClasses" class="btn btn-secondary">Quay lại</a>
        </div>
    </form>
</div>
