@model Qltt.Models.Class
@{
    ViewData["Title"] = "EditClass";
}

<div class="container mt-4">
    <h2 class="text-center">Sửa thông tin lớp học</h2>
    <form asp-action="EditClass" method="post">
        <div class="form-group">
            <label>Tên lớp</label>
            <input type="text" name="className" class="form-control" />
        </div>
        @* <div class="form-group">
            <label>Giáo viên</label>
            <select name="teacherEmail" class="form-control">
                @foreach (var teacher in ViewBag.Teachers)
                {
                    <option value="@teacher?.Email">@teacher?.firstName @teacher?.lastName</option>
                }
            </select>
        </div> *@
        <div class="mt-3">
            <button type="submit" class="btn btn-primary">Lưu</button>
            <a asp-action="ManageClasses" class="btn btn-secondary">Quay lại</a>
        </div>
    </form>
</div>
